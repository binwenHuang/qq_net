<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="edu.cdp.qq">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />

    <application
        android:name=".qq.MyApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".qq.Activity.LoadingActivity_temp"></activity>
        <activity android:name=".qq.Activity.VideoActicity"></activity>
        <activity android:name=".qq.Activity.SetingActivity" />
        <activity android:name=".qq.Activity.Msg_Info_Activity" />
        <activity android:name=".qq.Activity.RegisterActivity" />
        <activity android:name=".qq.Activity.MainActivity"></activity>
        <activity android:name=".qq.Activity.XYActivity" />
        <activity android:name=".qq.Activity.ForgetActivity" />
        <activity android:name=".qq.Activity.SetNetActivity" />
        <activity android:name=".qq.Activity.welcomeActivity"></activity>
        

        <activity android:name=".qq.Activity.LoadingActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>